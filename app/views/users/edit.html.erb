<h1 class='text-3xl text-amber-dark text-center mb-5'>会員情報編集</h1>
<div class='w-2/3 md:w-1/2 rounded-lg border border-amber-dark text-amber-dark bg-amber-card pt-2 m-auto shadow-md'>
  <h2 class="text-xl text-center mb-2">編集フォーム</h2>
  <div class="bg-white rounded-b-lg px-10 py-12">
    <%= form_with model: @user, local: true do |f| %>
      <%= render 'shared/error_messages', object: @user %>
        <div class="form-group mb-5">
          <%= f.label :name, class: 'block text-amber-dark font-bold mb-1 pr-4' %>
          <%= f.text_field :name, class: 'bg-white border-2 border-amber-dark rounded w-full py-2 px-4 text-gray-700 focus:outline-none focus:border-purple-500' %>
        </div>
        <%= f.submit '編集', class: 'px-3 py-2 bg-green-300 text-white rounded-lg' %>
    <% end %>
  </div>
</div>
<div class='mt-5 text-center'>
  <%= link_to '戻る', user_path(@user), class: 'px-3 py-2 bg-gray-200 text-amber-dark rounded-lg' %>
</div>